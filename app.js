//est variable es de tipo string//
let saludo = 'hola' ;

//esta variable es de tipo number//
let number = 9

//esta variable  es de tipo booleano//
let veradero = true 

// esta variable de es de tipo indefinida//
let indefinido = undefined 

// esta variable es de tipo nulo//
let nulo = null

// esta variable es una suma//
let suma = 2 + 5;

// esta variable es una resta //
let resta = 3 - 3

let multiplicaciOn = 50 * Number.MAX_SAFE_INTEGER ;
  
let division = 2 / 2 ;

let modulo = 2000 % 100
console.log(modulo);



 //Operadores logicos// :  < (menor que), > (mayor que), <= (menor igual) , >= ( Mayor igual que) 
 
// operadpres logicos // : SI (&&) Y NO (II)
